const amdz = require('adm-zip');
const fs = require('fs');
const path = require('path');

function comprimeArquivos() {
  
    
}

module.exports = {
    file_after_download: 'file.zip',
    data: this.data,
    tamanho: this.length,
    comprimeArquivos
}